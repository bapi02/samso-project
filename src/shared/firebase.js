// Firebase RTDB client — wired up in Step 4.
// Keep this file as the single import surface for both screen and controller.

export const ROOM_ID = 'default';

export function initFirebase() {
  // TODO(step-4): initializeApp + getDatabase, return { app, db }
  console.warn('[shared/firebase] initFirebase stub — Step 4 will implement');
  return null;
}
